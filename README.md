# Library_WEB_API

It's the ASP.NET Core RESTful API project that provides you a website (generated by Swagger UI) where you can perform different HTTP-requests to library database (GET, POST, PUT, DELETE, OPTIONS, HEAD). Choose the type of request body, and response body: JSON, XML.
